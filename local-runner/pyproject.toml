[project]
name = "qa-use-local-runner"
version = "0.1.0"
description = "Local browser-use runner for QA Use - replaces cloud API with local execution"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "browser-use>=0.2.0",
    "pydantic-settings>=2.7.0",
    "langchain-openai>=0.3.0",
    "langchain-anthropic>=0.3.0",
    "langchain-google-genai>=2.1.0",
]

[tool.uv]
dev-dependencies = []
